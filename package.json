{
  "name": "dream-journal-ultimate-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:llms": "node scripts/generate-llms.js",
    "build": "npm run generate:llms && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}